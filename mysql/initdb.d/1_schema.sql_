use api;

CREATE TABLE book (
    id VARCHAR(32) NOT NULL,
    title VARCHAR(32) NOT NULL,
    label VARCHAR(32),
    PRIMARY KEY (id)
);

INSERT INTO book (id,title,label) VALUES ('st.sol.18', 'solr starting','storage');
